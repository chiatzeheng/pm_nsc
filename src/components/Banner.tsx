import styled from "@emotion/styled";
import { Paper } from "@mui/material";

export default styled(Paper)(() => {
  return {
    padding: "1.5rem",
    position: "relative",
    borderRadius: "1rem",
  };
});
